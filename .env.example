# Frontend — copie para .env.local (gitignore; não commitar)
# VERSION: v1.0.2

PORT=5000

# Padrão: API no backend local (npm run backend na pasta backend, porta 5050)
REACT_APP_API_URL=http://localhost:5050

# --- Testar /hook e Observador com fluxo real (Octadesk → prod) ---
# Produção (Cloud Run): https://temporeal-278491073220.us-east1.run.app/
# 1) Na Octadesk, webhook: https://temporeal-278491073220.us-east1.run.app/api/integrations/octadesk/webhook
# 2) No PC, crie .env.local (sem commitar) com a base da API, sem barra final:
# REACT_APP_API_URL=https://temporeal-278491073220.us-east1.run.app
# 3) npm start — /hook e /observador usam a API de produção e o mesmo Mongo dos webhooks.
